version: '3'
services:
  clickservice:
    build:
      context : .
      dockerfile : click.Dockerfile
    ports:
      - "8080:8080"
  viewservice:
    build:
      context : .
      dockerfile: view.Dockerfile
    ports:
      - "9000:8080"
  impressionservice:
    build:
      context: .
      dockerfile: impression.Dockerfile
    ports:
      - "9020:8080"